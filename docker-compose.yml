# Thin forwarder so `docker compose up` still works from the repo root.
# Canonical stack lives in docker/compose.yaml (single source of truth).

name: amtechat

include:
  - path: docker/compose.yaml